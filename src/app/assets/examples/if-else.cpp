
// Conditional Structures
// Select the whole code to see the conditional structures as a graph

int a = 5;
int b = 4;
string msg;
if ( a > b ) {
    msg = "a > b";
} else if ( a < b ) {
    msg = "a < b";
} else {
    msg = "a == b";
}
return(msg);