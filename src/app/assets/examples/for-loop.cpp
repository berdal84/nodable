// For loop
// Select the whole code to see the conditional structures as a graph

double sum   = 0;
double count = 5;
for(double i = 1; i <= count; i = i+1)
{
    sum = sum + i;
}
bool check = sum == 15;