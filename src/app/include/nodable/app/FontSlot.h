#pragma once

typedef int FontSlot;

/**
 * Enum to identify each font slots need to run Nodable
 */
enum FontSlot_
{
    FontSlot_Paragraph,
    FontSlot_Heading,
    FontSlot_Code,
    FontSlot_ToolBtn,
    FontSlot_COUNT
};