language: cpp

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.8
    - g++-4.8
    - libusb-1.0-0-dev
    - libsdl2-dev
    - blibxrandr-dev
    - libxi-dev
    - libxxf86vm-dev

script:
  - make
