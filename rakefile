
require_relative "rakefile.tools"

task :default => :help

desc "Build the whole project"
task :build => ['tools:core:build', 'tools:gui:build']

desc "says hello"
task :help do
	print("Welcome to Nodable's rakefile.\nFor more info, run: rake --tasks\n")
end